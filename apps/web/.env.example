# URL del backend para el build del frontend.
# En desarrollo se deja vacío: Vite usa el proxy /api definido en vite.config.ts.
# En producción (Vercel) apunta al backend desplegado, por ejemplo:
#   VITE_API_URL=https://tu-backend.up.railway.app
VITE_API_URL=
